# Тема 3. Операторы, условия, циклы
Отчет по Теме #3 выполнил(а):
- Хайрутдинов Линар Рустамович
- АИС-23-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | + |
| Задание 5 | + | + |
| Задание 6 | + |
| Задание 7 | + |
| Задание 8 | + |
| Задание 9 | + |
| Задание 10 | + |

знак "+" - задание выполнено; знак "-" - задание не выполнено;



## Лабораторная работа №1
### Создайте две переменные, значение которых будете вводить через консоль. Также составьте условие, в котором созданные ранее переменные будут сравниваться, если условие выполняется, то выведете в консоль «Выполняется», если нет, то «Не выполняется»

```python
one = int(input('Введите значение первой переменной: '))
two = int(input('Введите значение второй переменной: '))
if one >=two:
    print('Выполняется')
else:
    print('Не выполняется')
```
### Результат.
![Скриншот выполнения](pic/lab3_1.png)

## Выводы

Данный код представляет собой программу сравнения двух целых чисел. Она запрашивает у пользователя два числовых значения и определяет, является ли первое число больше или равным второму.
Ввод данных - получение двух целых чисел от пользователя

Сравнение - проверка условия one >= two

Ветвление - вывод результата по одному из двух путей:

Если условие истинно → "Выполняется"

Если условие ложно → "Не выполняется"

## Лабораторная работа №2
### Напишите программу, которая будет определять значения переменной меньше 0, больше 0 и меньше 10 или больше 10. Это нужно реализовать при помощи одной переменной, значение которой будет вводится через консоль, а также при помощи конструкций if, elif, else
```python
one = int(input('Введите значение переменной: '))
if one > 0:
    print('Переменная больше 0')
elif 0 < one < 10:
    print('Переменная больше 0 и меньше10')
else:
    print ('Переменная больше 10')
```
### Результат.
![Скриншот выполнения](pic/lab3_2.png)
## Выводы

Данный код представляет собой программу классификации целых чисел по трем категориям. Она анализирует введенное пользователем число и определяет, к какому диапазону оно принадлежит.
Ввод данных - получение целого числа от пользователя
Многоуровневая проверка - последовательный анализ числа по трем условиям:
Проверка на положительное значение
Проверка на попадание в диапазон 0-10
Обработка всех остальных случаев
Классификация - отнесение числа к одной из трех категорий

## Лабораторная работа №3
### Напишите программу, в которой будет проверяться есть ли переменная в указанном массиве используя логический оператор in. Самостоятельно посмотрите, как работает программа со значениями которых нет в массиве numbers.
```python
numbers = [1, 3, 4, 6, 8, 9]
value = int(input('Введите значение переменной: '))
if value in numbers:
    print('Переменная есть в этом массиве')
else:
    print('Переменной нет в этом массиве')
```
### Результат.
![Скриншот выполнения](pic/lab3_3.png)
## Выводы
Данный код представляет собой программу поиска элемента в массиве. Она проверяет, присутствует ли введенное пользователем число в заранее заданном списке значений.
Инициализация данных - создание массива с фиксированными значениями [1, 3, 4, 6, 8, 9]
Ввод данных - получение целого числа от пользователя
Поиск элемента - проверка наличия значения в массиве с помощью оператора in
Бинарный результат - вывод одного из двух сообщений о результате поиска

## Лабораторная работа №4
### Напишите программу, которая будет определять находится ли переменная в указанном массиве и если да, то проверьте четная она или нет. Самостоятельно протестируйте данную программу с разными значениями переменной value.
```python
numbers = [1, 3, 4, 6, 8, 9, 15, 16, 73, 321, 322]
value = int(input('Введите значение переменной: '))
if value in numbers:
    if value % 2 == 0:
        print('Переменная чётная и есть в массиве numbers')
    else:
        print('Переменная нечётная и есть в массиве numbers')
else:
    print(f"Переменной нет в массиве numbers и она равна {value} ")
```
### Результат.
![Скриншот выполнения](pic/lab3_4.png)
## Выводы
Данный код представляет собой программу комплексного анализа числа с двойной проверкой. Он не только ищет число в массиве, но и анализирует его свойства (четность/нечетность), предоставляя расширенную информацию о результате.
Инициализация данных - создание расширенного массива с 11 значениями
Ввод данных - получение целого числа от пользователя
Первичная проверка - определение наличия числа в массиве
Вторичный анализ - для найденных чисел дополнительная проверка четности
Дифференцированный вывод - три различных сценария вывода информации

## Лабораторная работа №5
### Напишите программу, в которой циклом for значения переменной i будут меняться от 0 до 10 и посмотрите, как разные виды сравнений и операций работают в цикле.
```python
for i in range(10):
    print('i = ', i)
    if i == 0:
        i+=2
    if i ==1:
        continue
    if i ==2 or i ==3:
        print('Переменная равна 2 или 3')
    elif i in [4, 5, 6]:
        print('Переменная равна 4, 5 или 6')
    else:
        break
```
### Результат.
![Скриншот выполнения](pic/lab3_5.png)
## Выводы
Данный код представляет собой программу демонстрации различных механизмов управления циклом. Он показывает работу операторов continue, break и модификацию переменной цикла в процессе выполнения.
Итерация цикла - 10 запланированных итераций от 0 до 9

Модификация счетчика - принудительное изменение переменной i при определенных условиях

Управление потоком - использование continue для пропуска итераций и break для досрочного выхода

Классификация значений - группировка чисел по диапазонам с выводом соответствующих сообщений

## Лабораторная работа №6
### Напишите программу, в которой при помощи цикла for определяется есть ли переменная value в строке string и посмотрите, как работает оператор else для циклов. Самостоятельно посмотрите, что выведет программа, если значение переменной value оказалось в строке string. Определять индекс буквы не обязательно, но если вы хотите, то это делается при помощи строки: index = string.find(value) Вы берете название переменной, в которой вы хотите что-то найти, затем применяете встроенный метод find() и в нем указываете то, что вам нужно найти. Данная строка вернет индекс искомого объекта.
```python
string = 'Привет всем изучающим Python!'
value = input()
for i in string:
    if i == value:
        index = string.find(value)
        print(f"Буква '{value}' есть в строке под {index} индексом")
        break
else:
        print(f" Буквы '{value}' нет в указанной строке")

```
### Результат.
![Скриншот выполнения](pic/lab3_6.png)
## Выводы

Оптимизация поиска: Цикл прерывается при первом найденном совпадении

Двойной поиск: Используется как поэлементное сравнение, так и метод find()

Специфика Python: Блок else для цикла - уникальная особенность языка

Эффективность: Поиск может быть оптимизирован - метод find() сам выполняет поиск индекса
## Лабораторная работа №7
### Напишите программу, в которой вы наглядно посмотрите, как работает цикл for проходя в обратном порядке, то есть, к примеру не от 0 до 10, а от 10 до 0. В уже готовой программе показано вычитание из 100, а вам во время реализации программы будет необходимо придумать свой вариант применения обратного цикла
```python
value = 100
for i in range (10, -1, -1):
    value -= i
    print(i, value)
```
### Результат.
![Скриншот выполнения](pic/lab3_7.png)
## Выводы
Обратный порядок выполнения
Цикл проходит от 10 до 0 с шагом -1 с помощью range(10, -1, -1), где:
10 - начальное значение
-1 - конечное значение (не включается)
-1 - шаг уменьшения

## Лабораторная работа №8
### Напишите программу используя цикл while, внутри которого есть какие-либо проверки, но быть осторожным, поскольку циклы while при неправильно написанных условиях могут становится бесконечными, как указано в примере далее.
```python
value = 0
while value < 100:
    if value == 0:
        value +=10
    elif value // 5 > 1:
        value *= 5
    else:
        value -=5
    print(value)
```
### Результат.
![Скриншот выполнения](pic/lab3_8.png)
## Выводы
 Многоуровневое условное ветвление
Код использует три различных правила изменения переменной value:

Первая итерация: value += 10 (переход от 0 к 10)

Основная логика: value *= 5 (при value // 5 > 1)

Резервное правило: value -= 5 (во всех остальных случаях)

## Лабораторная работа №9
### Напишите программу с использованием вложенных циклов и одной проверкой внутри них. Самое главное, не забудьте, что нельзя использовать одинаковые имена итерируемых переменных, когда вы используете вложенные циклы
```python
value = 0
for i in range(10):
    for j in range(10):
        if i != j:
            value += j
        else:
            pass
print(value)
```
### Результат.
![Скриншот выполнения](pic/lab3_9.png)
## Выводы
Код использует два вложенных цикла for, каждый от 0 до 9:

Внешний цикл: for i in range(10) - 10 итераций

Внутренний цикл: for j in range(10) - 10 итераций для каждого i

Общее количество итераций: 100

Итоговая сумма = 405

## Лабораторная работа №10
### Напишите программу с использованием flag, которое будет определять есть ли нечетное число в массиве. В данной задаче flag выступает в роли индикатора встречи нечетного числа в исходном массиве четных чисел
```python
even_array = [2, 4, 6, 8, 9]
flag = False
for value in even_array:
    if value %2 == 1:
        flag = True
if flag is True:
    print('В массиве есть нечетное число')
else:
    print('В массиве все числа четные')
```
### Результат.
![Скриншот выполнения](pic/lab3_10.png)
## Выводы
Код проверяет массив even_array, который по названию должен содержать только четные числа

Используется условие value % 2 == 1 для обнаружения нечетных чисел

При нахождении первого нечетного числа устанавливается флаг flag = True
Переменная flag инициализируется как False

При обнаружении нечетного числа флаг переключается в True

После завершения цикла проверяется состояние флага для финального вывода
## Самостоятельная работа №1
### Напишите программу, которая преобразует 1 в 31. Для выполнения поставленной задачи необходимо обязательно и только один раз использовать: • Цикл for • *= 5 • += 1 Никаких других действий или циклов использовать нельзя
```python
one = 1
for i in range(2):
    one *= 5
    one += 1
print(one)

```
![Скриншот выполнения](pic/sam3_1.png)
## Выводы
Двухэтапное преобразование в цикле
Каждая итерация цикла выполняет две операции:

Умножение на 5: one *= 5

Прибавление 1: one += 1

Цикл выполняется 2 раза (range(2))
## Самостоятельная работа №2
### Напишите программу, которая фразу «Hello World» выводит в обратном порядке, и каждая буква находится в одной строке консоли.
```python
message = "Hello World"
for list in reversed(message):
    print(list)
```
![Скриншот выполнения](pic/sam3_2.png)
## Выводы
reversed(message) - переворачивает строку

for list in - проходит по каждому символу

print(list) - выводит каждый символ на новой строке

Программа выводит все символы фразы "Hello World" в обратном порядке, каждый на отдельной строке.
## Самостоятельная работа №3
### Напишите программу, на вход которой поступает значение из консоли, оно должно быть числовым и в диапазоне от 0 до 10 включительно (это необходимо учесть в программе). Если вводимое число не подходит по требованиям, то необходимо вывести оповещение об этом в консоль и остановить программу. Код должен вычислять в каком диапазоне находится полученное число. Нужно учитывать три диапазона: • от 0 до 3 включительно • от 3 до 6 • от 6 до 10 включительно Результатом работы программы будет выведенный в консоль диапазон. Программа должна занимать не более 10 строчек в редакторе кода.
```python
p = int(input('Введите переменную'))
if p < 0 or p > 10:
    print('Число не в диапазоне от 0 до 10, введи другое')
    exit()
if p <= 3:
    print('Число в диапазоне от 0 до 3включительно')
elif 3 < p <= 6:
    print('Число в диапазоне от 3 до 6')
else:
    print('Число в диапазоне от 6 до 10 включительно')
```
![Скриншот выполнения](pic/sam3_3.png)
## Выводы
Краткая суть кода:
Проверяет число на диапазон 0-10
Определяет один из трёх диапазонов:
0-3
3-6
6-10

Выводит соответствующий диапазон числа
## Самостоятельная работа №4
### Манипулирование строками. Напишите программу на Python, которая принимает предложение (на английском) в качестве входных данных от пользователя. Выполните следующие операции и отобразите результаты: • Выведите длину предложения. • Переведите предложение в нижний регистр. • Подсчитайте количество гласных (a, e, i, o, u) в предложении. • Замените все слова "ugly" на "beauty". • Проверьте, начинается ли предложение с "The" и заканчивается ли на "end". Проверьте работу программы минимум на 3 предложениях, чтобы охватить проверку всех поставленных условий
```python
pred = input("Введите предложение на английском: ")
print(f"Длина предложения: {len(pred)}")
print(f"В нижнем регистре: {pred.lower()}")
glas = sum(1 for kolv in pred.lower() if kolv in 'aeiou')
print(f"Количество гласных: {glas}")
change = pred.replace('ugly', 'beauty')
print(f"После замены 'ugly' на 'beauty': {change}")
start = pred.startswith('The')
ends = pred.endswith('end')
print(f"Начинается с 'The': {start}")
print(f"Заканчивается на 'end': {ends}")
```
![Скриншот выполнения](pic/sam3_4.png)
## Выводы
Что делает:
Длина строки
Нижний регистр
Считает гласные
Заменяет ugly на beauty
Проверяет начало/конец
## Самостоятельная работа №5
### Составьте программу, результатом которой будет данный вывод в консоль:
```python
string = 'hello'
memory = 'world'
counter = 0
values = [0, 2, 4, 6, 8, 10]
while counter != 10:
    if counter in values:
        print(string + ' ' + memory)
    else:
        print(string)
    counter += 1
print(string + ' world')
```
![Скриншот выполнения](pic/sam3_5.png)
## Выводы
При четных counter (0,2,4,6,8) выводит "hello world"

При нечетных counter (1,3,5,7,9) выводит "hello"

В конце добавляется дополнительная строка "hello world"

## Общие выводы по теме
Основные темы и концепции:
1. Работа с циклами
Прямые и обратные циклы for
Вложенные циклы
Управление потоком (break, continue)
Модификация переменных в циклах

2. Условные операторы
Простые условия if-else
Множественные ветвления if-elif-else
Вложенные условия
Проверка диапазонов и принадлежности

3. Работа со строками
Обратный вывод символов
Поиск подстрок и символов
Замена текста
Проверка начала/конца строки
Подсчет гласных
В общем и целом было интуитивно интересно, спасибо за практику
